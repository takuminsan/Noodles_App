$(".post_comments_list").append("<%= j(render 'comment',{ comment: @comment }) %>");
$(".post_comments_form textarea").val('');
$("#comment_count").html('<i class="far fa-comments"></i> コメント（<%= @post.comments.count %>）');
