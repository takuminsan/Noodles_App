<!DOCTYPE html>
<html>
  <head>
    <title>Noodles</title>
    <script src="https://maps.googleapis.com/maps/api/js?key=<%= Rails.application.credentials.GCP[:API_KEY] %>" async defer></script>
    <script defer src="https://use.fontawesome.com/releases/v5.12.0/js/all.js"></script>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <%= render 'layouts/rails_default' %>
    <%= render 'layouts/shim' %>
  </head>
  <body>
    <%= render 'layouts/header' %>
    <main>
      <% flash.each do |message_type, message| %>
        <div class="alert alert-<%= message_type %> col-md-12"><%= message %></div>
      <% end %>
      <%= yield %>
    </main>
  <%= render 'layouts/footer' %>
  </body>
</html>
