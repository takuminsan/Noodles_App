<% provide(:title, '利用者一覧') %>
<div class="users">
  <h3>利用者一覧</h3>
  <ul class="users_index">
    <% @users.each do |user| %>
      <%= render 'users/user', user: user %>
    <% end %>
  </ul>
  <%= will_paginate %> <!-- @usersオブジェクトを自動で見つけ出し、ページネーションリンクを作成する -->
</div>
